export const denmarkLandmarkComponent = (landmarkObj) => {
    return `
                    <li>Landmark: ${landmarkObj.landmark}</li>
                    <div>Location: ${landmarkObj.location}</div><br>
                    
    `
}